apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "solar-webui.fullname" . }}-env
  labels:
    {{- include "solar-webui.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
