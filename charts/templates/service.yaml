apiVersion: v1
kind: Service
metadata:
  name: {{ include "solar-webui.fullname" . }}
  labels:
    {{- include "solar-webui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "solar-webui.selectorLabels" . | nindent 4 }}
